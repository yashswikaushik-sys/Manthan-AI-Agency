'use strict';

// Clients are added exclusively via the UI onboarding flow (client-onboarding agent)
// and stored in Airtable (Clients table) + Notion + SQLite (client_contexts).
// This file intentionally starts empty — no hardcoded client data.

module.exports = [];
